const place = "World";
const greeting = "Hello";

console.log(greeting + " " + place); // concatenation of a string and two variables
console.log("Hello World"); // string literal
console.log(`Hello ${place}`); // template string, makes it easier to see where the variable values are
console.log("%s %s", greeting, place); // template literal, can be a little harder to read than template strings
